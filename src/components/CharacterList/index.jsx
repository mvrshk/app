import React from 'react';
import CharacterCard from '../CharacterCard';

const CharacterList = () => {
    return (
        <main>
        <div class="cards">
        <CharacterCard/>
        </div>
    </main>
    );
}

export default CharacterList;

